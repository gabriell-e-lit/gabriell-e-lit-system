Author {
    id: string,                     // уникален идентификатор: "ivan-ivanov"
    name: string,                   // пълно име
    biography: string|null,         // кратка биография
    roles: [string],                // "poet", "writer", "translator", "editor", др.

    portrait: string|null,          // Artwork.id (портрет)
    gallery: [string],              // Artwork.id[] (илюстрации, снимки, картини)
    exhibitions: [string],          // Exhibition.id[] (ако има)

    books: [string],                // Book.id[] (книги на автора)
    issues: [string],               // Issue.id[] (в кои броеве участва)
    posts: [string],                // Post.id[] (публикации в списанието)

    source_urls: [string],          // реални URL-и, от които е извлечена информация
    wp_tag_url: string|null,        // WordPress таг на автора (не страница!)
    static_author_url: [string]|null,// статични ресурси (портрети, изложби)

    order: number|null              // позиция в списък/серия (ако има)
}
